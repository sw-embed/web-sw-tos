Vendored sw-tos tools/te-rs/src/protocol.rs (commit 9fed3b7) into a new crates/swtos-frontend, unchanged apart from a provenance header. The module has zero use statements so it needs no dependencies, and all 7 of its tests pass unmodified, which is itself the proof the copy is faithful. Also carried out the risk-first saga reorder Mike approved: protocol lands before the pump so the throughput unknown is answered before any renderer is written. Accepted a deliberate sw-checklist regression (23/1/3, was 21/0/0) on the vendored file rather than splitting it and forking it from upstream; recorded the rule and the rejected alternatives in docs/architecture.md and in a sw-checklist: exception trailer.