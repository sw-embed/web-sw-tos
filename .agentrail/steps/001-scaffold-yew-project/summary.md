Scaffolded the Yew/Trunk/WASM project: Cargo.toml (edition 2024, cdylib+rlib, Yew 0.21 CSR, cor24-emulator/cor24-isa path deps), build.rs build provenance, lib.rs/main.rs/footer.rs/app.css, index.html with Catppuccin Mocha and favicon.ico, serve.sh, build-pages.sh (--public-url /web-sw-tos/), and the Pages workflow. Proved the emulator crate is wasm-clean at compile time (libcor24_emulator.rlib builds for wasm32-unknown-unknown); noted it is a build check only since std::fs and SystemTime::now() would only fail at runtime and run_batch is not exercised until step 005. Recorded the deliberate three-crate workspace tree in docs/architecture.md to stay inside sw-checklist's module limits. Built and committed pages/ ahead of the step 008 deploy so the Pages workflow has an artifact on every push. sw-checklist 16/0/0.