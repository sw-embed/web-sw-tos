Re-vendored sw-tos through 525fbe2 across several rounds and closed the debugger-demo parity gaps against the new docs/use-cases.md: ! shell passthrough, Ctrl-A l clear, Ctrl-A S and mon autostart arriving free via the vendored command table, the built-in monitor pane fed by RESOURCE_SNAPSHOT, UPTIME/WALL_CLOCK ticks, runtime debug-map fetch, and the status clock. Fixed four defects found by driving the live demo: the status clock never set, dis/list reporting no map, the menu loop from re-offering HELLO after negotiation, and Ctrl-A ? dead because a Shift keydown consumed the prefix.