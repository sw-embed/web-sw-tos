Shipped Mike's 256x256 desktop-monitor favicon.ico to the live demo, replacing the 16x16 scaffold placeholder, and rebuilt pages/. The relocation this step was opened for was reverted: sw-checklist's favicon check requires the file at the repo root, and Mike confirmed the root location is fine, so a symlink workaround was dropped. Verified end to end by serving pages/ under the real /web-sw-tos/ public path and matching SHA-256 across the source file, the hashed bundle file, and the bytes served over HTTP. sw-checklist unchanged at 16/0/0.