Deployed the interactive shell demo and verified it against the live URL rather than localhost: pressing 5 on https://sw-embed.github.io/web-sw-tos/ runs the multitasking program and shows two processes interleaving (B1, C1, B2, C2). Refreshed images/screenshot.png from the live page and rewrote the README's status and usage sections, which still described the step-001 scaffold. Recorded that build-pages.sh fails while a trunk serve is running because they race the dist directory.