Wired the Ctrl-A prefix. Desktop::command already implemented the command set; this was the missing wiring, needing neither framing nor vendored-file changes. Ctrl-A then ?/z/1-9/n/p/y/x/e now work, copy mode claims navigation keys, and the status line becomes the command menu while the prefix is armed since the bindings were otherwise undiscoverable. Reproducing Mike's report found that bare Escape already stopped Uptime (discoverability, not capability, was missing) and exposed a real regression of mine: local echo pushed raw control bytes into the pane, rendering Escape as U+FFFD. Verified live: prefix menu appears, Ctrl-A z zooms 4 panes to 1 and back, Ctrl-A e returns Uptime to READY, no replacement characters.