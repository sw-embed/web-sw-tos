Re-vendored from sw-tos d6dbce9: image crc24:30c68b (24,870 bytes, sixteen slots, per-process channels, heap in SRAM) plus ui.rs, resource.rs and debug.rs. protocol.rs was unchanged upstream and needed no patch. Deleted all three clippy patches -- upstream cleared its own findings and solved too_many_arguments with a BoxSpec struct rather than the local Rect. Converted the ui.rs patch from an invasive char-to-Cell rewrite into a thin render_grid adapter over upstream's render, after upstream grew ui.rs by 375 lines; the adapter survived untouched and picked up the new separator chrome for free, and all 23 vendored tests now run completely unmodified. Re-measured the payload bound against the new image rather than trusting it: exactly 16, sharp at 17. Verified live -- transport framed, menu 9 fills all sixteen slots and the TTY panes populate themselves now that every process has its own channel.