Replaced the raw text dump with the vendored pane desktop: four tiled panes, focus marker, status line, output routed through the real transport decoder as StreamItem::Plain. Geometry selectable 80x24 / 120x43, verified by measuring rendered rows and widths. Fixed a livelock introduced in step 008: an Interval kept firing while each 16ms tick overran its 10ms request, so callbacks queued and the main thread never yielded; the tick is now self-rescheduling and bounded by a 50ms wall-clock budget with catch-up retained for throttled tabs. Throughput improved 16.45 -> 12.4 ms/tick. Merged footer into chrome.rs and step_many into run_until to keep self-authored checklist findings at zero.