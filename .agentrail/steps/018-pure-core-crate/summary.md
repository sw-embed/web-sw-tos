Restructured toward the design sw-checklist is a forcing function for, after Mike corrected both my preference for a documented exception and my framing that the validator caused the bugs. Extracted crates/swtos-session (pure) and crates/swtos-input (keyboard, layered on it); injected time as a Clock trait with the browser implementing it and tests supplying a fake; separated declarations from behaviour so state.rs has no impl blocks; composed Session from Transport/Panes/Input/Console; lib.rs files carry only mod statements. The root crate is now only the browser, with browser.rs the sole file touching js_sys. Zero self-authored sw-checklist failures, and four new tests cover paths that previously needed a browser -- including the HELLO guard whose absence caused the menu loop.