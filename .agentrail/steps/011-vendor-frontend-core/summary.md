Vendored ui.rs, resource.rs, and debug.rs from sw-tos 9fed3b7, completing the frontend core. Three adaptations: resource.rs off std::time (Instant -> caller-supplied Millis, since Instant::now panics on wasm32), debug.rs off std::fs (load(path) -> from_json), and ui.rs rendering a Cell grid instead of an ANSI string, which is the decision that keeps ANSI colour additive rather than a rewrite. All 20 upstream tests pass unmodified, with a helper flattening the cell grid so the vendored assertions still check the same behaviour. Fixed 5 clippy findings rather than suppressing any, 3 of them in vendored code. Recorded in docs/architecture.md why clippy is fixed everywhere but sw-checklist's structural limits are excepted for vendored files. sw-checklist 22/9/13, the regression entirely vendored.