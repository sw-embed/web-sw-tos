Vendored the SWTOS image (program.bin, 21,890 bytes) and debug map (program.debug.json, 1.6 MB) from sw-tos da3bc82 build/scheduled-shell, and created crates/swtos-host as the workspace's second crate. image.rs embeds the image with include_bytes! and mirrors build_id crc24:b2202e, image_size, and image_sha256 as constants; two tests assert the embedded image still matches the map, so a half-updated pair fails rather than misreporting symbols. Decided NOT to compile the debug map into the library: at 1.6 MB it would dwarf the ~126 KB bundle and, since pages/ is committed, would add that bulk to git history on every rebuild; only the debugger pane needs it and can fetch it as a static asset. Wrote assets/PROVENANCE.md and scripts/refresh-image.sh, which re-copies, verifies the pair, and warns on a dirty source tree. sw-checklist 21/0/0.